package project.view.pooyaviewpackage;

//public class hfxbcgnj {
//}
        //sequentialTransition.play();
        /*
        AdjustCardsInHandTransition adjustCardsInHandTransition = new AdjustCardsInHandTransition();
        SequentialTransition sqt = new SequentialTransition(cardMovingTransition, adjustCardsInHandTransition);
        sqt.play();
        */
//allyCardsInDeckGroup.getChildren().remove(cardView);
//}
        /*
        cardLocationLookingRightNow = giveCardLocationByCoordinateInView(x, y);
        if (cardLocationLookingRightNow != null) {
            String output = GameManager.getDuelControllerByIndex(0).getInput("select " + giveStringToGiveToServerByCardLocation(cardLocationLookingRightNow), true);
            System.out.println("!" + output);
            output = GameManager.getDuelControllerByIndex(0).getInput("card show --selected", true);
            System.out.println("*" + output);
        }

         */
//System.out.println("cardlocation " + cardLocation.getRowOfCardLocation() + " i


//translate.play();
//Instantiating Sequential Transition class by passing the list of transitions into its constructor
//SequentialTransition seqT = new SequentialTransition(cardView, translate);

//playing the transition

//seqT.play();
/*
            if (allyCardsInHandGroup.getChildren().size() == 1) {
                SequentialTransition seqT = new SequentialTransition(cardView, translate);
                sequentialTransition.getChildren().add(seqT);
            } else {
                SequentialTransition seqT = new SequentialTransition(cardView, translate);
                sequentialTransition.getChildren().add(seqT);
                ParallelTransition pt = new ParallelTransition();
                for (int j = 0; j < allyCardsInHandGroup.getChildren().size(); j++) {

                    //if (j != i) {
                    TranslateTransition translateNow = new TranslateTransition(Duration.millis(1000), (CardView) allyCardsInHandGroup.getChildren().get(j));
                    translateNow.setToX(45);
                    translateNow.setCycleCount(1);
                    translateNow.setAutoReverse(true);
                    pt.getChildren().add(translateNow);

                    //}

                }
                //seqT.getChildren().add(pt);
                sequentialTransition.getChildren().add(pt);


                //pt.getChildren().add(pause);
                //pt.play();
                //SequentialTransition seqT = new SequentialTransition(cardView, translate, pt);
                //sequentialTransition.getChildren().add(seqT);
            }

 */
